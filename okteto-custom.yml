name: test-pipeline

deploy:
  commands:
    - name: echo something
      command: echo "something"
    - name: echo variable
      command: echo "printing external variable... ${EXTERNAL_VARIABLE}"

destroy:
  commands:
    - name: echo
      command: echo "destroying...."
    - name: echo variable
      command: echo "printing external variable... ${EXTERNAL_VARIABLE}"
    - name: fail
      command: non-existent-comment
